# =============================================================================
# Open Event Orchestrator - Environment Variables
# =============================================================================
# Copy this file to .env and configure the values.
# Most settings can also be configured via the Admin UI at /admin/settings
# =============================================================================

# -----------------------------------------------------------------------------
# PocketBase (Required)
# -----------------------------------------------------------------------------
# URL of your PocketBase instance
PUBLIC_POCKETBASE_URL=http://localhost:8090

# PocketBase admin credentials (for seeding/initialization scripts only)
PB_ADMIN_EMAIL=admin@pocketbase.local
PB_ADMIN_PASSWORD=adminpassword123

# -----------------------------------------------------------------------------
# Stripe (Optional - for paid tickets)
# -----------------------------------------------------------------------------
# Can also be configured via Admin UI at /admin/settings/stripe
# Get your keys at https://dashboard.stripe.com/test/apikeys
#
# For local development, use Stripe test mode (free) + Stripe CLI for webhooks:
#   docker compose --profile stripe up
# The CLI forwards webhook events to your local app automatically.

# Secret key (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=sk_test_your_key_here

# Publishable key (starts with pk_test_ or pk_live_)
PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Webhook signing secret (starts with whsec_)
# For local dev: displayed by stripe-cli on startup
# For production: configure at https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=

# Stripe API base URL (leave empty to use real Stripe API)
STRIPE_API_BASE=

# -----------------------------------------------------------------------------
# HelloAsso (Optional - alternative to Stripe)
# -----------------------------------------------------------------------------
# HelloAsso is configured via the Admin UI (Organization Settings), not env vars.
# Required settings (set in Admin UI):
#   - Client ID
#   - Client Secret
#   - Organization Slug
#   - API Base URL (https://api.helloasso.com or https://api.helloasso-sandbox.com)
#
# To use HelloAsso, set "Active Payment Provider" to "helloasso" in Admin UI.
# See docs/payments.md for full setup guide.

# -----------------------------------------------------------------------------
# SMTP (Optional - for emails)
# -----------------------------------------------------------------------------
# Can also be configured via Admin UI at /admin/settings
# If not configured, emails will be logged to console

# For local development with Mailpit:
# SMTP_HOST=localhost
# SMTP_PORT=1025

# For production (example with SendGrid):
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM=noreply@your-domain.com

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@open-event-orchestrator.local

# -----------------------------------------------------------------------------
# Integrations (Configured via Admin UI)
# -----------------------------------------------------------------------------
# The following integrations are configured via the Admin UI:
#
# Discord: /admin/settings/discord
#   - Webhook URL for notifications
#   - Enable/disable specific notification types
#
# Slack: /admin/settings/slack
#   - Webhook URL for notifications
#   - Channel and username configuration
#   - Enable/disable specific notification types
#
# These settings are stored in the PocketBase database (app_settings collection)
# and do not require environment variables.

# -----------------------------------------------------------------------------
# Application (Optional)
# -----------------------------------------------------------------------------
# Port for the frontend application (default: 3000)
PORT=3000

# Node environment
NODE_ENV=development
