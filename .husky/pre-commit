# Run lint-staged for formatting staged files
pnpm lint-staged

# Note: full lint (pnpm lint) and full test suite (pnpm test) are enforced by CI.
# Running 5755+ tests in pre-commit is too slow and causes timeout issues.
# lint-staged already runs biome check --fix on staged files.
